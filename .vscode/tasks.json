{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build-extension",
      "type": "shell",
      "command": "bun run vsc:build",
      "options": {
        "env": {
          "PATH": "${env:HOME}/.bun/bin:${env:PATH}"
        }
      },
      "group": "build",
      "presentation": {
        "reveal": "always"
      }
    }
  ]
}
