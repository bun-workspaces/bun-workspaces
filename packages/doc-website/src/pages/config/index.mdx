---
title: General | Configuration
titleSuffix: " | bun-workspaces"
description: "Configuration documentation for bun-workspaces, such as the bw.json config file | Documentation for bun-workspaces: A CLI and API to help manage Bun monorepos"
pageType: doc
sidebar: false
outline: true
footer: false
---

import { ROOT_CONFIG_FILE_NAME, ROOT_CONFIG_PACKAGE_JSON_KEY } from "bun-workspaces/src/config/rootConfig/rootConfigLocation";
import {  exampleWorkspaceConfigArray, exampleWorkspaceConfigSimple, createPackageJsonExample } from "../../lib/config/exampleConfig";
import { SyntaxHighlighter } from "../../lib/util/highlight";
// import { WORKSPACE_CONFIG_FILE_PATH, WORKSPACE_CONFIG_PACKAGE_JSON_KEY} from 'bun-workspaces/src/config/workspaceConfig/workspaceConfigLocation'
import { CONFIG_LOCATION_TYPES, createConfigLocationPath } from 'bun-workspaces/src/config/util/configLocation'
import { ROOT_CONFIG_TYPE } from "../../lib/config/configTypes";
import { ConfigHeader } from "../../lib/config/ConfigHeader";

# Configuration > General

<ConfigHeader activeHref="home" divider/>

Configuration is set by JSON. JSONC is supported in either `.jsonc` or `.json` files.

See the [Root Configuration](/config/root) and [Workspace Configuration](/config/workspace) for more information on the different types of configuration.

## Precedence (Resolution Order)

### Configuration locations

Configuration is read from the following locations, in order of precedence:
<ol className="config-location-list">
{CONFIG_LOCATION_TYPES.map(type => <li key={type}><code>{createConfigLocationPath(type, "bw.<config name>", "<key>")}</code></li>)}
</ol>

The first configuration location found will be used.

### Value Resolution

Values related to configuration are resolved in the following order of precedence:
1. An explicit CLI or API argument/option passed to a method/function/command
2. A value provided in a configuration file
3. A value provided via an [environment variable](/config/env-vars), if applicable
4. The generic default value, if applicable

