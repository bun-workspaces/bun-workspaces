---
title: Workspace Configuration
titleSuffix: " | bun-workspaces"
description: "Workspace configuration documentation for bun-workspaces | Documentation for bun-workspaces: A CLI and API to help manage Bun monorepos"
pageType: doc
sidebar: false
outline: true
footer: false
---

import { ROOT_CONFIG_FILE_NAME, ROOT_CONFIG_PACKAGE_JSON_KEY, WORKSPACE_CONFIG_FILE_NAME, WORKSPACE_CONFIG_PACKAGE_JSON_KEY } from "bun-workspaces/src/config";
import {  exampleWorkspaceConfigArray, exampleWorkspaceConfigSimple, createPackageJsonExample } from "../../../lib/config/exampleConfig";
import { SyntaxHighlighter } from "../../../lib/util/highlight";
import { CONFIG_LOCATION_TYPES, createConfigLocationPath } from 'bun-workspaces/src/config/util'
import { ROOT_CONFIG_TYPE, WORKSPACE_CONFIG_TYPE } from "../../../lib/config/configTypes";
import { ConfigHeader } from "../../../lib/config/ConfigHeader";  

# Configuration > Workspace

<ConfigHeader activeHref="workspace"/>

## Workspace Configuration

Configuration for a workspace is defined in a file in its directory or in its `package.json` file.

See [workspace aliases](/concepts/workspace-aliases) and [script execution order](/concepts/script-execution-order) for more information on the fields available.

### Type Definition

<SyntaxHighlighter language="typescript">
  {WORKSPACE_CONFIG_TYPE}
</SyntaxHighlighter>

### Examples:
#### 1. JSON/JSONC file

<span className="config-path"><code>path/to/your/workspace/{createConfigLocationPath("jsonFile", WORKSPACE_CONFIG_FILE_NAME, WORKSPACE_CONFIG_PACKAGE_JSON_KEY)}</code></span>
or
<span className="config-path"><code>path/to/your/workspace/{createConfigLocationPath("jsoncFile", WORKSPACE_CONFIG_FILE_NAME, WORKSPACE_CONFIG_PACKAGE_JSON_KEY)}</code></span>

<SyntaxHighlighter language="json">
  {JSON.stringify(exampleWorkspaceConfigSimple, null, 2)}
</SyntaxHighlighter>

#### 2. package.json

<span className="config-path"><code>path/to/your/workspace/package.json</code></span>

<SyntaxHighlighter language="json">
  {JSON.stringify(createPackageJsonExample(exampleWorkspaceConfigSimple, "workspace"), null, 2)}
</SyntaxHighlighter>
