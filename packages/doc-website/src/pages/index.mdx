---
pageType: doc
sidebar: false
outline: false
---

import { Bwunster } from "../lib/components/Bwunster";
import { SyntaxHighlighter } from "../lib/util/highlight";
import { API_QUICKSTART } from '../lib/api/apiExamples';
import { CLI_QUICKSTART } from '../lib/cli/cliExamples';
import { Footer } from "../lib/components/Footer";
import { AnimatedSprite, PixelArtImage } from "../lib/util/pixelArt";
import { RequiredBunVersion } from "../lib/components/RequiredBunVersion";
import { SmorsicBanner } from "../lib/components/SmorsicBanner";
import { WebCliButton } from "../lib/web-cli/WebCliButton";
import { Banner } from "../lib/components/Banner";
import { BlogLink } from "../lib/components/BlogLink";

<Banner />

<Bwunster/>

<h1 className="home-title" id="home-title">
  bun-workspaces
</h1>

<h2 className="home-subtitle">
  A [monorepo](http://sonarsource.com/resources/library/monorepo/) management tool on top <br/>of native [Bun
  workspaces](https://bun.sh/docs/install/workspaces)
</h2>

<div className="home-list-container">
  <ul className="home-list">
    <li>Works right away, with no boilerplate required 🍔🍴</li>
    <li>Get metadata about your monorepo 🤖</li>
    <li>Orchestrate your workspaces' `package.json` scripts 📋</li>
    <li>Run inline [Bun Shell](https://bun.com/docs/runtime/shell) scripts in your workspaces 🐚</li>
  </ul>
</div>

<WebCliButton />

This is a tool to help manage a Bun monorepo, offering features beyond what Bun's [`--filter` feature](https://bun.com/docs/pm/filter) can do. It can be used to get a variety of metadata about your project and run scripts across your workspaces with advanced control.

To get started, all you need is a repo using [Bun's workspaces feature](https://bun.sh/docs/install/workspaces) for nested JavaScript/TypeScript packages.

This package is unopinionated and works with any project structure you want. Think of this as a power suit you can snap onto native workspaces, rather than whole new monorepo framework.

Start running some [CLI commands](/cli) right away in your repo, or take full advantage of the [scripting API](/api) and its features.
<br/>
<hr />

<h2 className="whats-new-title">What's New</h2>

<h2 className="whats-new-subtitle">Notable recent updates 📦</h2>
<div className="whats-new-list">
- Workspaces can be grouped by tags, defined in [configuration](/config/workspace) and usable in [workspace patterns](/concepts/workspace-patterns).
- <BlogLink>The `bun-workspaces` Blog</BlogLink> is now live! The <BlogLink path="bun-workspaces-v1">first post</BlogLink> covers the v1 release.
- The optional [configuration files](/config) can now be written in TypeScript or JavaScript.
- New CLI [global option](/cli/global-options#run-from-workspace-root): `--workspace-root|-w` to run from the project root above the current working directory, like `pnpm`'s option.
- <span style={{ fontWeight: "500", fontSize: "1.05em"}}><span style={{ fontWeight: "800"}}>Version 1.0</span> is now here after the initial alpha! 🍔🍔👁️🍔🍔</span>
- New CLI [output style](/cli/commands#run-script) for running scripts. `--no-prefix` is now <span className="deprecated">deprecated</span> in favor of `--output-style=plain`.
- You can now run scripts in dependency order, similar to Bun's [`--filter` behavior](https://bun.com/docs/pm/filter#dependency-order). See more on [workspace dependencies](/concepts/workspace-dependencies).
</div>

## Quick Start

See the [CLI Quick Start](/cli) and [API Quick Start](/api) for starting examples. See the [Glossary](/concepts/glossary) for more fundamental concepts.

## Installation

<RequiredBunVersion className="home-bun-version bun-version" />


You can install the package in your project or simply use `bunx bun-workspaces` to immediately use the latest version of the [CLI](/cli). Installation is required to use the [API](/api).

<SyntaxHighlighter language="bash">
{`# Install to use the API and/or lock your CLI version for your project
bun add --dev bun-workspaces

# Start using the CLI with or without the installation step
bunx bun-workspaces --help`}

</SyntaxHighlighter>

If you use the CLI frequently, you might consider using a shell alias.

<SyntaxHighlighter language="bash">
  {`# Add this to .bashrc, .zshrc, or similar 
# to make it available by default in your shell
alias bw="bunx bun-workspaces"`}
</SyntaxHighlighter>

Examples in these docs will use the alias `bw` for `bunx bun-workspaces` for brevity.

Note that you need to run `bun install` in your project for `bun-workspaces` to find your project's workspaces. This is because it reads `bun.lock`. This also means that if you update your workspaces, such as changing their name, you must run `bun install` for the change to reflect.

<br/>
_`bun-workspaces` is independent from the [Bun](https://bun.sh) project and is not affiliated with or endorsed by Anthropic. This project aims to enhance the experience of Bun for its users._

<div className="home-smorsic-banner">
  <span className="home-smorsic-banner-text">Developed By:</span>
  <SmorsicBanner width="280px" />
</div>

