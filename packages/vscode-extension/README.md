## Development

- Use `F5` to run the development window through the VSCode debugger.
- Use the `dev` script (can run `bun vsc:dev` from the repo root) to run the development window via shell directly to the `./devProject` directory.
